# Cliente vigilante

El cliente vigilante recibe los frames inferidos por el servidor de testeo a través de un socket en ```http://localhost:3000``` y los muestra en una tabla con la información que corresponde a la inferencia del objeto

## Ejecución

```
# Instalar dependencias
npm install

# Ejecutar la aplicación web
node app.js

```